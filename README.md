# ระบบติดตามสภาพแวดล้อม IoT สำหรับอุตสาหกรรม (Industrial IoT Environmental Monitoring System)

ระบบติดตามและตรวจสอบสภาพแวดล้อมแบบเรียลไทม์ที่ออกแบบมาสำหรับงานอุตสาหกรรม ระบบนี้สามารถติดตามค่าอุณหภูมิ (Temperature), ความชื้น (Humidity), ระดับก๊าซโอโซน (Ozone - O3) และสถานะน้ำรั่วซึม (Water Leakage) โดยใช้เซ็นเซอร์ LoRaWAN ระบบจะประมวลผลข้อมูลผ่าน Node-RED บันทึกลงฐานข้อมูล Microsoft SQL Server และแสดงผลผ่าน Grafana Dashboard ที่ครอบคลุมทั้งการปฏิบัติงาน การซ่อมบำรุง และการออกรายงาน

## 🏗 สถาปัตยกรรมระบบ (System Architecture)

การทำงานของระบบเป็นไปตามมาตรฐาน IoT Data Pipeline ดังนี้:
1.  **Sensors:** อุปกรณ์ตรวจวัด Milesight IoT (รุ่น EM320-TH, EM500-PT100, WS303, O3)
2.  **Transport:** ส่งข้อมูลผ่านโปรโตคอล MQTT (Uplink topics)
3.  **Middleware (Node-RED):**
    * รับข้อมูลและแปลงรูปแบบ (Data Normalization)
    * ตรวจสอบเงื่อนไขการแจ้งเตือน (Logic & Thresholds)
    * จัดการการตั้งค่าอุปกรณ์ผ่านหน้าเว็บ (Device Management UI)
4.  **Storage:** จัดเก็บข้อมูลลง **Microsoft SQL Server (MSSQL)**
5.  **Visualization:** แสดงผลข้อมูลผ่าน **Grafana** (แยกตามวัตถุประสงค์การใช้งาน)
6.  **Notification:** แจ้งเตือนผ่าน HTTP API ไปยัง Email Server

## 📦 สิ่งที่ต้องเตรียม (Prerequisites)

ก่อนเริ่มติดตั้ง ตรวจสอบให้แน่ใจว่าได้ติดตั้งและตั้งค่าสิ่งเหล่านี้แล้ว:
* **Node-RED** พร้อมติดตั้ง Palette ต่อไปนี้:
    * `node-red-contrib-mssql-plus` (เชื่อมต่อฐานข้อมูล)
    * `node-red-dashboard` หรือ `node-red-contrib-ui-base` (Dashboard 2.0 สำหรับหน้า Admin)
    * `node-red-contrib-ui-form` (แบบฟอร์มตั้งค่า)
* **Microsoft SQL Server** (ฐานข้อมูลหลัก)
* **Grafana** (สำหรับแสดงผล Dashboard)
* **MQTT Broker** (รับข้อมูลจาก Gateway)

## ⚙️ การทำงานของ Node-RED (Logic & Workflows)

Flow ใน Node-RED ถูกออกแบบให้ทำงานเป็นระบบอัตโนมัติ โดยแบ่งฟังก์ชันหลักออกเป็น 4 ส่วน:

### 1. Site Cache & Configuration (การจัดการการตั้งค่า)
* **การทำงาน:** เมื่อเริ่มระบบ (Startup) หรือทุกๆ 1 ชั่วโมง ระบบจะ Query ข้อมูลจากตาราง `site_detail` ในฐานข้อมูล
* **Global Context:** ข้อมูลจะถูกแปลงเป็น Object `site_map` เก็บไว้ในหน่วยความจำ (RAM) เพื่อใช้:
    * จับคู่ชื่ออุปกรณ์ (`deviceName`) เป็นรหัสสถานที่ (`site_id`)
    * ดึงค่าการตั้งค่าเฉพาะของอุปกรณ์นั้นๆ เช่น ค่าวิกฤต (**Min/Max**), ระยะเวลาหน่วงการแจ้งเตือน (**Interval**), และสถานะยกเว้นการแจ้งเตือน (**Skip**)

### 2. Data Processing (การรับและประมวลผลข้อมูล)
* **Input:** รับข้อมูลจาก MQTT Topic `milesight/uplink/#`
* **Normalization:** ฟังก์ชันจัดการข้อมูลทำหน้าที่:
    * แปลง Timezone ของ Gateway ให้เป็นเวลาท้องถิ่น
    * ตรวจสอบระดับแบตเตอรี่
    * **Logic การแจ้งเตือน:** เปรียบเทียบค่าที่วัดได้จริงกับค่า `min_a`/`max_a` (Temp/O3) หรือ `min_b`/`max_b` (Humidity) ที่ดึงมาจาก Cache
    * **Rate Limiting:** ตรวจสอบค่า `interval` เพื่อป้องกันการส่งแจ้งเตือนซ้ำๆ ในเวลาสั้นๆ
    * **Skip Mode:** ตรวจสอบว่าอุปกรณ์ถูกตั้งค่าให้ข้ามการแจ้งเตือนหรือไม่ (เช่น ช่วงซ่อมบำรุง)
* **Storage:** บันทึกข้อมูลดิบลงตารางแยกตามประเภทอุปกรณ์ (`em320_th_data`, `em500_pt100_data`, `ws303_data`, `o3_data`)

### 3. Alerting System (ระบบแจ้งเตือน)
* **เงื่อนไข:** ทำงานเมื่อ Logic ในส่วนที่ 2 พบความผิดปกติ (`Alert = true`)
* **การสร้างข้อความ:** สร้างเนื้อหาแจ้งเตือนในรูปแบบ HTML Template ที่สวยงามและรองรับการแสดงผลบนมือถือ พร้อมระบุหัวข้อตามระดับความรุนแรง (High/Low)
* **การส่งข้อมูล:** ส่งข้อมูลผ่าน **HTTP Request** ไปยัง API ภายใน เพื่อดำเนินการส่งอีเมลจริง

### 4. Admin Device Management (หน้าจัดการอุปกรณ์)
* **User Interface:** ใช้ **Node-RED Dashboard 2.0** สร้างตารางรายการอุปกรณ์
* **In-Line Editing:** ผู้ดูแลระบบสามารถแก้ไขค่าการตั้งค่าได้จากหน้าตารางโดยตรง:
    * ปรับค่า Min/Max Threshold
    * ตั้งค่า Interval (นาที) สำหรับหน่วงการแจ้งเตือน
    * เปิด/ปิดโหมด Skip (Maintenance)
* **Real-time Sync:** เมื่อกดบันทึก ระบบจะอัปเดตข้อมูลลงฐานข้อมูล MSSQL และสั่งให้ Node-RED โหลด Cache ใหม่ทันที เพื่อให้การตั้งค่ามีผลต่อการแจ้งเตือนในทันที

## 📊 แดชบอร์ด Grafana (Dashboard Overview)

ระบบประกอบด้วยไฟล์ JSON สำหรับ Grafana จำนวน 3 ไฟล์ เพื่อตอบโจทย์ผู้ใช้งานแต่ละกลุ่ม:

### 1. Operations Dashboard 
* **กลุ่มผู้ใช้:** ฝ่ายผลิต, ผู้จัดการโรงงาน
* **ฟีเจอร์หลัก:**
    * **Canvas Floor Plan:** แผนผังโรงงานแสดงตำแหน่งติดตั้งจริง พร้อมสถานะไฟกระพริบ (เขียว/แดง) ตามเงื่อนไขการแจ้งเตือน
    * **Real-time Monitoring:** กราฟแสดงค่าอุณหภูมิ ความชื้น และโอโซน แบบเรียลไทม์ แยกตามโซน (Indoor/Outdoor)
    * **Batch Integration:** ตารางแสดงข้อมูล Batch การผลิต เชื่อมโยงกับค่าเฉลี่ยสภาพแวดล้อม (Temp Avg, O3 Avg) ในช่วงเวลานั้นๆ

### 2. Device Management Dashboard (ไฟล์: `Manage devices.json`)
* **กลุ่มผู้ใช้:** ฝ่ายไอที, ทีมซ่อมบำรุง
* **ฟีเจอร์หลัก:**
    * **System Health:** สรุปจำนวนเซ็นเซอร์ทั้งหมด แยกตาม Gateway (Gateway 1, Gateway 2)
    * **Online/Offline Status:** ระบบจะนับว่าอุปกรณ์ **Offline** หากไม่มีการส่งข้อมูลเข้ามาเกิน **30 นาที**
    * **Battery Monitoring:** ตารางและเกจวัดแสดงระดับแบตเตอรี่ของเซ็นเซอร์ทุกตัว เพื่อวางแผนการเปลี่ยนถ่านบำรุงรักษา

### 3. Reporting Dashboard (ไฟล์: `Export batch data.json`)
* **กลุ่มผู้ใช้:** ฝ่ายควบคุมคุณภาพ (QA), ฝ่ายวิเคราะห์ข้อมูล
* **ฟีเจอร์หลัก:**
    * **Historical Data Table:** ตารางแสดงข้อมูลย้อนหลังแบบละเอียด เน้นข้อมูลราย Batch
    * **Data Export:** ออกแบบมาเพื่อการดึงข้อมูล (Query) เฉพาะช่วงเวลาหรือเลขที่ Batch เพื่อนำไปวิเคราะห์ต่อ หรือ Export เป็นไฟล์ CSV/Excel

## 🔧 การแก้ไขปัญหาเบื้องต้น (Troubleshooting)

* **เวลาไม่ตรง (Timezone):** ในคำสั่ง SQL (ทั้งใน Node-RED และ Grafana) มีการใช้ฟังก์ชัน `DATEADD(HOUR, -7, ...)` เพื่อปรับเวลา โปรดตรวจสอบเวลาของ Database Server เทียบกับเวลาท้องถิ่น (UTC+7) และปรับค่านี้ให้เหมาะสม
* **ข้อมูลไม่ขึ้น:**
    * ตรวจสอบ Debug window ใน Node-RED หากพบข้อความ "Device not found in list" ให้ตรวจสอบชื่อ `deviceName` ใน MQTT Payload ว่าตรงกับ `site_name` ในฐานข้อมูลหรือไม่ (ตัวอักษรพิมพ์เล็ก-ใหญ่มีผล)
    * ตรวจสอบการเชื่อมต่อ MQTT Broker
* **การแจ้งเตือนไม่ส่ง:**
    * ตรวจสอบการตั้งค่า API Endpoint ใน Node-RED (โหนด HTTP Request)
    * ตรวจสอบค่า **Interval** ในหน้า Admin ว่าตั้งไว้สูงเกินไปหรือไม่ (อาจทำให้ติด Rate limit)

---
